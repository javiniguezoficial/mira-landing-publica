import { RecoverPasswordPage } from '@/components/landing/RecoverPasswordPage'

export const metadata = {
  title: 'Recuperar contraseña — Mira Pricing',
}

export default function RecuperarPassword() {
  return <RecoverPasswordPage />
}
